# FUSE mount artifacts from in-place edits
.fuse_hidden*

# routeTree.gen.ts is COMMITTED (not ignored): CI and fresh clones type-check
# without running the vite plugin. The dev server regenerates it; commit the
# churn or discard it, either is fine.

node_modules/
.output/
.vinxi/
.tmp/
dist/
.env
.env.local
.env.*.local
*.log
