/**
 * RETIRED — the in-browser esbuild preview engine has been removed.
 *
 * Modal cloud sandboxes are the sole preview engine (see preview-panel.tsx).
 * This file has no imports and no exports; it is an inert tombstone kept only
 * because the workspace mount blocks file deletion. Safe to `git rm` outside
 * this environment.
 */
export {};
